# WebUI（FastAPI + ffmpeg）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 移除对旧桌面 GUI（PyQt/Tkinter）的默认依赖与主流程曝光，新增一个本机单用户 WebUI（FastAPI）来驱动现有人脸检测/马赛克能力，并支持“连续帧、输出分辨率、码率、检测库”等常用参数。

**Architecture:** WebUI 进程内直接调用核心处理逻辑（方案 B）。处理时先用现有 OpenCV 写出中间视频，再用 ffmpeg 按用户选择做转码（分辨率/码率/编码器），最终产物落到 `outputs/` 并在页面中列出与下载。一次只允许运行一个 job。

**Tech Stack:** Python 3.8+、FastAPI、Uvicorn、Jinja2、SSE（Server-Sent Events）、ffmpeg（外部可执行文件）、OpenCV/NumPy、（可选）DeepFace。

---

## 代码结构（落地后）

**新增目录/文件（建议）**
- Create: `vfd/__init__.py`
- Create: `vfd/job_types.py`（Job/参数/状态类型）
- Create: `vfd/job_runner.py`（核心：运行一次视频处理任务）
- Create: `vfd/ffmpeg.py`（ffmpeg 探测与转码）
- Create: `vfd/webui/__init__.py`
- Create: `vfd/webui/app.py`（FastAPI 应用与路由）
- Create: `vfd/webui/job_store.py`（单 job 存储、日志与事件队列）
- Create: `vfd/webui/templates/index.html`
- Create: `vfd/webui/templates/job.html`
- Create: `vfd/webui/templates/outputs.html`
- Create: `vfd/webui/static/app.js`（如需少量 JS；否则可选用纯 SSE + minimal JS）
- Create: `vfd/webui/README.md`（WebUI 使用说明：启动、参数、ffmpeg 安装）
- Create: `outputs/.gitkeep`（确保目录存在）

**修改文件**
- Modify: `requirements.txt`（移除 PyQt5/argparse，保留核心依赖）
- Create: `requirements-webui.txt`（webui 依赖）
- Create: `requirements-dev.txt`（测试/开发依赖）
- Modify: `README.md`（主入口改为 CLI + WebUI，旧 GUI 标注 deprecated）
- Modify: `docs/GUI_USAGE_GUIDE.md`（开头标注 Deprecated，并指向 WebUI）

---

## Task 1: 依赖与目录基建

**Files:**
- Modify: `requirements.txt`
- Create: `requirements-webui.txt`
- Create: `requirements-dev.txt`
- Create: `outputs/.gitkeep`

- [ ] **Step 1: 调整核心依赖（从默认路径移除 GUI 依赖）**

将 `requirements.txt` 调整为只包含核心依赖（示例）：

```txt
opencv-python==4.8.1.78
numpy==1.24.3
```

说明：
- `argparse` 属于标准库，删除。
- `PyQt5` 将迁移到 legacy（不再默认安装）。

- [ ] **Step 2: 新增 WebUI 依赖清单**

创建 `requirements-webui.txt`（示例）：

```txt
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
```

- [ ] **Step 3: 新增开发/测试依赖清单**

创建 `requirements-dev.txt`（示例）：

```txt
pytest>=8.0.0
```

- [ ] **Step 4: 新增 outputs 目录占位**

创建 `outputs/.gitkeep`，并确保 `.gitignore` 忽略 `outputs/*.mp4`、`outputs/.tmp/` 等产物（如已忽略则不改）。

- [ ] **Step 5: 验证依赖可安装**

Run:

```bash
python3 -m pip install -r requirements.txt -r requirements-webui.txt -r requirements-dev.txt
```

Expected: 安装成功，无 PyQt5 相关依赖被拉起。

- [ ] **Step 6: Commit**

```bash
git add requirements.txt requirements-webui.txt requirements-dev.txt outputs/.gitkeep .gitignore
git commit -m "chore: add webui and dev requirements"
```

---

## Task 2: 定义 Job 类型与参数规范（单 Job）

**Files:**
- Create: `vfd/job_types.py`
- Test: `tests/test_job_types.py`

- [ ] **Step 1: 编写失败测试（参数解析与归一化）**

创建 `tests/test_job_types.py`：

```python
from vfd.job_types import normalize_bitrate, normalize_output_resolution


def test_normalize_bitrate_accepts_mbps_int():
    assert normalize_bitrate("6") == "6M"


def test_normalize_bitrate_accepts_ffmpeg_style():
    assert normalize_bitrate("6000k") == "6000k"
    assert normalize_bitrate("6M") == "6M"


def test_normalize_output_resolution_presets():
    assert normalize_output_resolution("original") is None
    assert normalize_output_resolution("720p") == (1280, 720)
    assert normalize_output_resolution("1080p") == (1920, 1080)


def test_normalize_output_resolution_custom():
    assert normalize_output_resolution("1024x768") == (1024, 768)
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
pytest -q
```

Expected: FAIL（`vfd.job_types` 不存在）。

- [ ] **Step 3: 实现最小类型定义与归一化函数**

创建 `vfd/job_types.py`（示例骨架，必须包含函数级注释/docstring）：

```python
from __future__ import annotations

from dataclasses import dataclass
from enum import Enum
from typing import Optional, Tuple


class JobStatus(str, Enum):
    queued = "queued"
    running = "running"
    succeeded = "succeeded"
    failed = "failed"


@dataclass(frozen=True)
class JobParams:
    input_path: str
    detector: str
    deepface_backend: str
    continuation_frames: int
    apply_mosaic: bool
    mosaic_size: int
    output_resolution: Optional[Tuple[int, int]]
    video_bitrate: Optional[str]
    codec: str


def normalize_bitrate(value: Optional[str]) -> Optional[str]:
    """
    归一化码率输入，返回 ffmpeg 可接受的格式（如 6000k、6M）。
    """
    if value is None:
        return None
    v = value.strip()
    if v == "":
        return None
    if v.lower().endswith(("k", "m")):
        return v
    if v.isdigit():
        return f"{v}M"
    raise ValueError("invalid bitrate")


def normalize_output_resolution(value: str) -> Optional[Tuple[int, int]]:
    """
    归一化输出分辨率输入。
    - original -> None（表示不缩放）
    - 720p/1080p -> 固定 WxH
    - 1024x768 -> 自定义 WxH
    """
    v = value.strip().lower()
    if v in ("original", ""):
        return None
    if v == "720p":
        return (1280, 720)
    if v == "1080p":
        return (1920, 1080)
    if "x" in v:
        w, h = v.split("x", 1)
        return (int(w), int(h))
    raise ValueError("invalid resolution")
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```bash
pytest -q
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add vfd/job_types.py tests/test_job_types.py
git commit -m "feat: add job types and parameter normalization"
```

---

## Task 3: ffmpeg 探测与转码封装

**Files:**
- Create: `vfd/ffmpeg.py`
- Test: `tests/test_ffmpeg.py`

- [ ] **Step 1: 编写失败测试（命令行拼装）**

创建 `tests/test_ffmpeg.py`：

```python
from vfd.ffmpeg import build_transcode_cmd


def test_build_transcode_cmd_with_scale_and_bitrate():
    cmd = build_transcode_cmd(
        input_path="/tmp/in.mp4",
        output_path="/tmp/out.mp4",
        output_resolution=(1280, 720),
        video_bitrate="6M",
        codec="h264",
    )
    joined = " ".join(cmd)
    assert "-vf" in cmd
    assert "scale=1280:720" in joined
    assert "-b:v" in cmd
    assert "6M" in cmd
    assert "libx264" in joined
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
pytest -q
```

Expected: FAIL（`vfd.ffmpeg` 不存在）。

- [ ] **Step 3: 实现 ffmpeg 工具模块**

创建 `vfd/ffmpeg.py`（示例骨架）：

```python
from __future__ import annotations

import shutil
from typing import List, Optional, Tuple


def find_ffmpeg() -> Optional[str]:
    """
    查找系统 ffmpeg 可执行文件路径；找不到返回 None。
    """
    return shutil.which("ffmpeg")


def build_transcode_cmd(
    input_path: str,
    output_path: str,
    output_resolution: Optional[Tuple[int, int]],
    video_bitrate: Optional[str],
    codec: str,
) -> List[str]:
    """
    构建 ffmpeg 转码命令（不执行），用于控制输出分辨率/码率/编码器。
    """
    cmd: List[str] = ["ffmpeg", "-y", "-i", input_path]

    if output_resolution is not None:
        w, h = output_resolution
        cmd += ["-vf", f"scale={w}:{h}"]

    if video_bitrate is not None:
        cmd += ["-b:v", video_bitrate]

    if codec == "h264":
        cmd += ["-c:v", "libx264"]
    else:
        cmd += ["-c:v", "copy"]

    cmd += ["-movflags", "+faststart", output_path]
    return cmd
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```bash
pytest -q
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add vfd/ffmpeg.py tests/test_ffmpeg.py
git commit -m "feat: add ffmpeg command builder"
```

---

## Task 4: 核心 Job Runner（进程内调用现有检测器）

**Files:**
- Create: `vfd/job_runner.py`
- Modify (optional): `main.py`（复用 runner，减少重复逻辑）
- Test: `tests/test_job_runner_validation.py`

- [ ] **Step 1: 编写失败测试（输入校验）**

创建 `tests/test_job_runner_validation.py`：

```python
import pytest

from vfd.job_runner import validate_input_path


def test_validate_input_path_missing_file():
    with pytest.raises(FileNotFoundError):
        validate_input_path("/path/does/not/exist.mp4")
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
pytest -q
```

Expected: FAIL（`vfd.job_runner` 不存在）。

- [ ] **Step 3: 实现最小 runner 与校验函数**

创建 `vfd/job_runner.py`（示例骨架）：

```python
from __future__ import annotations

import os
import pathlib
import time
from dataclasses import dataclass
from typing import Callable, Optional

from vfd.ffmpeg import build_transcode_cmd, find_ffmpeg
from vfd.job_types import JobParams


@dataclass(frozen=True)
class JobResult:
    output_path: str
    stats: dict


def validate_input_path(path: str) -> None:
    """
    校验输入视频路径，失败抛出异常，便于 WebUI 直接展示错误信息。
    """
    if not os.path.exists(path):
        raise FileNotFoundError(path)


def run_job(
    job_id: str,
    params: JobParams,
    outputs_dir: str,
    log: Callable[[str], None],
    progress: Callable[[int, int], bool],
) -> JobResult:
    """
    运行一次视频处理任务：
    1) 使用现有 OpenCV 流水线生成中间文件
    2) 若用户设置输出分辨率/码率，则用 ffmpeg 转码得到最终文件
    """
    validate_input_path(params.input_path)

    outputs = pathlib.Path(outputs_dir)
    tmp_dir = outputs / ".tmp"
    tmp_dir.mkdir(parents=True, exist_ok=True)
    outputs.mkdir(parents=True, exist_ok=True)

    tmp_path = str(tmp_dir / f"{job_id}.tmp.mp4")
    final_path = str(outputs / f"{job_id}.mp4")

    log("initializing detector")

    from face_detector import VideoFaceDetector

    try:
        from deepface_detector import HybridFaceDetector
        deepface_available = True
    except Exception:
        deepface_available = False

    if params.detector == "yunet":
        detector = VideoFaceDetector(continuation_frames=params.continuation_frames)
    elif params.detector in ("deepface", "hybrid"):
        if not deepface_available:
            raise RuntimeError("deepface not available")
        detector = HybridFaceDetector(
            primary_backend="yunet" if params.detector == "hybrid" else params.deepface_backend,
            enable_deepface=True,
            deepface_backend=params.deepface_backend,
            continuation_frames=params.continuation_frames,
        )
    else:
        raise ValueError("invalid detector")

    started = time.time()

    stats = detector.process_video(
        input_path=params.input_path,
        output_path=tmp_path,
        show_preview=False,
        apply_mosaic=params.apply_mosaic,
        mosaic_size=params.mosaic_size,
        progress_callback=progress,
        codec="auto",
    )

    log(f"opencv stage done in {time.time() - started:.2f}s")

    need_transcode = params.output_resolution is not None or params.video_bitrate is not None or params.codec == "h264"
    if not need_transcode:
        os.replace(tmp_path, final_path)
        return JobResult(output_path=final_path, stats=stats)

    ffmpeg = find_ffmpeg()
    if ffmpeg is None:
        raise RuntimeError("ffmpeg not found")

    cmd = build_transcode_cmd(
        input_path=tmp_path,
        output_path=final_path,
        output_resolution=params.output_resolution,
        video_bitrate=params.video_bitrate,
        codec=params.codec,
    )
    cmd[0] = ffmpeg
    log("running ffmpeg transcode")

    import subprocess

    subprocess.run(cmd, check=True, capture_output=True, text=True)
    os.remove(tmp_path)
    return JobResult(output_path=final_path, stats=stats)
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```bash
pytest -q
```

Expected: PASS（至少校验测试通过；不跑真实视频处理）。

- [ ] **Step 5:（可选）让 CLI 复用 runner**

将 `main.py` 的 detector 初始化与处理逻辑逐步迁移为调用 `run_job(...)`，减少两套逻辑分叉。

- [ ] **Step 6: Commit**

```bash
git add vfd/job_runner.py tests/test_job_runner_validation.py main.py
git commit -m "feat: add in-process job runner"
```

---

## Task 5: WebUI Job Store（单 Job + SSE 事件流）

**Files:**
- Create: `vfd/webui/job_store.py`
- Test: `tests/test_job_store.py`

- [ ] **Step 1: 编写失败测试（单 job 锁）**

创建 `tests/test_job_store.py`：

```python
import pytest

from vfd.webui.job_store import JobStore


def test_job_store_single_job_only():
    store = JobStore()
    job1 = store.create_job(input_path="/tmp/a.mp4")
    assert job1.id
    with pytest.raises(RuntimeError):
        store.create_job(input_path="/tmp/b.mp4")
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
pytest -q
```

Expected: FAIL（`vfd.webui.job_store` 不存在）。

- [ ] **Step 3: 实现 JobStore**

创建 `vfd/webui/job_store.py`（示例骨架）：

```python
from __future__ import annotations

import threading
import time
import uuid
from dataclasses import dataclass, field
from typing import Deque, Dict, Optional
from collections import deque

from vfd.job_types import JobStatus


@dataclass
class JobState:
    id: str
    status: JobStatus
    created_at: float
    updated_at: float
    progress: int = 0
    logs: Deque[str] = field(default_factory=lambda: deque(maxlen=2000))
    output_path: Optional[str] = None
    error: Optional[str] = None


class JobStore:
    """
    单用户单任务的 Job 存储。
    """

    def __init__(self) -> None:
        self._lock = threading.Lock()
        self._job: Optional[JobState] = None

    def get(self) -> Optional[JobState]:
        """
        获取当前 job（没有则返回 None）。
        """
        with self._lock:
            return self._job

    def create_job(self, input_path: str) -> JobState:
        """
        创建一个新 job；若已有运行中的 job，则抛出异常。
        """
        with self._lock:
            if self._job is not None and self._job.status in (JobStatus.queued, JobStatus.running):
                raise RuntimeError("job already running")
            now = time.time()
            self._job = JobState(
                id=str(uuid.uuid4()),
                status=JobStatus.queued,
                created_at=now,
                updated_at=now,
            )
            self._job.logs.append(f"input={input_path}")
            return self._job
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```bash
pytest -q
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add vfd/webui/job_store.py tests/test_job_store.py
git commit -m "feat: add single-job store for webui"
```

---

## Task 6: WebUI FastAPI 应用（页面 + API + SSE）

**Files:**
- Create: `vfd/webui/app.py`
- Create: `vfd/webui/templates/index.html`
- Create: `vfd/webui/templates/job.html`
- Create: `vfd/webui/templates/outputs.html`
- Create: `vfd/webui/static/app.js`（如需要）

- [ ] **Step 1: 新增最小可启动 FastAPI**

`vfd/webui/app.py`（示例骨架）：

```python
from __future__ import annotations

import pathlib
from fastapi import FastAPI, Request
from fastapi.responses import HTMLResponse
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates

from vfd.webui.job_store import JobStore


def create_app() -> FastAPI:
    """
    创建 FastAPI 应用实例。
    """
    app = FastAPI()
    base_dir = pathlib.Path(__file__).resolve().parent
    templates = Jinja2Templates(directory=str(base_dir / "templates"))

    app.mount("/static", StaticFiles(directory=str(base_dir / "static")), name="static")

    store = JobStore()

    @app.get("/", response_class=HTMLResponse)
    def index(request: Request) -> HTMLResponse:
        return templates.TemplateResponse("index.html", {"request": request, "job": store.get()})

    return app
```

- [ ] **Step 2: 补齐路由**

实现：
- `POST /jobs`：读取表单参数 → 生成 `JobParams` → `store.create_job()` → 启动后台线程执行 `run_job()`
- `GET /jobs/{id}`：任务页
- `GET /api/jobs/{id}`：JSON 状态
- `GET /api/jobs/{id}/events`：SSE（推送 log/progress/status）
- `GET /outputs`：列出 `outputs/` 下 `.mp4`
- `GET /outputs/{filename}`：下载

SSE 推荐事件结构（JSON 文本）：
```json
{"type":"log","message":"..."}
{"type":"progress","value":42}
{"type":"status","value":"running"}
```

- [ ] **Step 3: 手工验证 WebUI 可启动**

Run:

```bash
python3 -c "from vfd.webui.app import create_app; import uvicorn; uvicorn.run(create_app(), host='127.0.0.1', port=7860)"
```

Expected: 终端打印服务启动日志；浏览器访问 `http://127.0.0.1:7860/` 能看到页面。

- [ ] **Step 4: Commit**

```bash
git add vfd/webui/app.py vfd/webui/templates vfd/webui/static
git commit -m "feat: add fastapi webui skeleton"
```

---

## Task 7: WebUI 与 JobRunner 集成（单 Job 串行执行）

**Files:**
- Modify: `vfd/webui/app.py`
- Modify: `vfd/webui/job_store.py`
- (optional) Create: `vfd/webui/forms.py`（表单解析/校验拆分）

- [ ] **Step 1: 在后台线程中执行 run_job**

实现细节要求：
- 创建 job 后立刻置为 `running`
- `log()` 回调：追加到 `job.logs`
- `progress(cur,total)` 回调：写入 `job.progress=int(cur/total*100)`；返回 True（v1 不做取消）
- 成功：置 `succeeded`，写入 `output_path`
- 失败：置 `failed`，写入 `error`（字符串）

- [ ] **Step 2: SSE 输出**

实现最小版本：
- 前端 `EventSource` 订阅 `/api/jobs/{id}/events`
- 服务端按增量把 `logs` 与 `progress/status` 推出（可用“日志游标”或“最后更新时间”策略）

- [ ] **Step 3: 端到端手工验证**

准备一个小视频，填入本机路径，选择：
- 检测库：`yunet`
- 连续帧：5
- 输出分辨率：720p
- 码率：6M

Expected:
- 页面显示进度与日志滚动
- `outputs/<job_id>.mp4` 生成
- 输出列表可下载

- [ ] **Step 4: Commit**

```bash
git add vfd/webui/app.py vfd/webui/job_store.py vfd/webui/templates/job.html vfd/webui/static/app.js
git commit -m "feat: wire webui to job runner with sse"
```

---

## Task 8: 文档与旧 GUI 弃用（对外只暴露 CLI + WebUI）

**Files:**
- Modify: `README.md`
- Modify: `docs/GUI_USAGE_GUIDE.md`
- Create: `vfd/webui/README.md`

- [ ] **Step 1: README 主入口改为 WebUI**

README 至少包含：
- WebUI 启动方式（`pip install -r ...` + `uvicorn` 命令）
- ffmpeg 安装（macOS：`brew install ffmpeg`）
- 常用参数解释（连续帧、输出分辨率、码率、检测库）
- 旧 GUI 标注 Deprecated，并指向 `docs/GUI_USAGE_GUIDE.md`

- [ ] **Step 2: 标注 GUI_USAGE_GUIDE Deprecated**

在文档开头增加醒目说明：Deprecated，不再维护，推荐使用 WebUI。

- [ ] **Step 3: Commit**

```bash
git add README.md docs/GUI_USAGE_GUIDE.md vfd/webui/README.md
git commit -m "docs: promote webui and deprecate legacy desktop gui"
```

---

## Task 9: 基础 CI（macOS 优先）

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: 新增 CI 工作流**

最低要求：
- 触发：push、pull_request
- runner：macos-latest
- 步骤：setup-python → pip install requirements* → pytest -q

- [ ] **Step 2: 验证 CI 本地等价命令**

Run:

```bash
python3 -m pip install -r requirements.txt -r requirements-webui.txt -r requirements-dev.txt
pytest -q
```

Expected: PASS。

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add macos workflow for tests"
```

---

## Spec 覆盖检查（对照需求）
- 方案 B：Task 4（run_job 进程内调用）+ Task 7（webui 集成）
- 单 job：Task 5（JobStore）+ Task 7（创建时拒绝并发）
- 连续帧：Task 4（JobParams.continuation_frames → detector 初始化）
- 输出分辨率：Task 2（normalize_output_resolution）+ Task 4/3（ffmpeg scale）
- 码率：Task 2（normalize_bitrate）+ Task 3（-b:v）+ Task 4（转码阶段）
- 检测库：Task 4（yunet/deepface/hybrid）
- 弃用旧 GUI：Task 8（README + docs）

